# cbor-logical-message-decoder

Logical message decoder using CBOR format instead of JSON.
Used for Kafka outbox pattern.

# Building

```
mvn install
```

# Testing docker image

Build the jar file first

```
cd testing-docker
./build.sh
```

